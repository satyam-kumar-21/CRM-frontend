'use client';

import { useEffect, useState } from 'react';
import { Building2, Check, CheckCircle2, CreditCard, Download, Globe, Pencil, Phone, Plus, Target, Trash2, UserCheck, UserPlus, TrendingUp, X } from 'lucide-react';
import { toast } from 'sonner';
import { companyService, ICompanyEmployee, ICompanyLead, ICompanySale } from '@/services/companyService'; import { getBusinessDateString, matchesBusinessDateFilters } from '@/lib/businessDate';
type LeadForm = Omit<ICompanyLead, '_id'>;
type SaleForm = Omit<ICompanySale, '_id'>;

const emptyLead: LeadForm = { name: '', country: '', system: '', contactNo: '', otherDetails: '', connected: 'no', connectedBy: 'Unassigned', customerType: 'NEW', isSale: 'no' };
const emptySale: SaleForm = { name: '', country: '', system: '', connectedBy: '', amount: 0, paymentMethod: 'Card', customerType: 'NEW', saleDate: getBusinessDateString() };
const inputClass = 'w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-xs text-white outline-none transition focus:border-indigo-500 focus:ring-2 focus:ring-indigo-500/20';
const selectClass = `${inputClass} cursor-pointer appearance-none bg-[linear-gradient(45deg,transparent_50%,#94a3b8_50%),linear-gradient(135deg,#94a3b8_50%,transparent_50%)] bg-[position:calc(100%-14px)_50%,calc(100%-9px)_50%] bg-[size:5px_5px,5px_5px] bg-no-repeat pr-8`;
type DateFilters = { employee: string; month: string; from: string; to: string; customer: string };
function DateFilterBar({ filters, setFilters, employees, employeeLabel, readOnly }: { filters: DateFilters; setFilters: (filters: DateFilters) => void; employees: ICompanyEmployee[]; employeeLabel: string; readOnly: boolean }) { return <div className="flex flex-wrap items-end gap-2 rounded-xl border border-slate-800 bg-slate-900/60 p-3"><span className="text-xs font-semibold text-slate-400">Filter</span>{!readOnly && <label className="text-[11px] text-slate-400">{employeeLabel}<select value={filters.employee} onChange={(event) => setFilters({ ...filters, employee: event.target.value })} className={`${selectClass} ml-1`}><option value="">Everyone</option>{employees.map((employee) => <option key={employee._id} value={employee.name}>{employee.name} · {employee.role}</option>)}</select></label>}<label className="text-[11px] text-slate-400">Customer<input type="text" value={filters.customer} onChange={(event) => setFilters({ ...filters, customer: event.target.value })} placeholder="Search by name" className={`${inputClass} ml-1`} /></label><label className="text-[11px] text-slate-400">Month & year<input type="month" value={filters.month} onChange={(event) => setFilters({ ...filters, month: event.target.value, from: '', to: '' })} className={`${inputClass} ml-1`} /></label><label className="text-[11px] text-slate-400">Start date<input type="date" value={filters.from} onChange={(event) => setFilters({ ...filters, from: event.target.value, month: '' })} className={`${inputClass} ml-1`} /></label><label className="text-[11px] text-slate-400">End date<input type="date" value={filters.to} onChange={(event) => setFilters({ ...filters, to: event.target.value, month: '' })} className={`${inputClass} ml-1`} /></label><button type="button" onClick={() => setFilters({ employee: '', month: '', from: '', to: '', customer: '' })} className="rounded-lg bg-slate-700 px-3 py-2 text-xs text-white">Clear</button></div>; }
const downloadCsv = (filename: string, rows: string[][]) => { const csv = rows.map((row) => row.map((cell) => `"${cell.replaceAll('"', '""')}"`).join(',')).join('\n'); const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([csv], { type: 'text/csv;charset=utf-8' })); link.download = filename; link.click(); URL.revokeObjectURL(link.href); };

function Field({ label, value, onChange, type = 'text' }: { label: string; value: string | number; onChange: (value: string) => void; type?: string }) {
  return <label className="block space-y-1 text-xs text-slate-400"><span>{label}</span><input className={inputClass} type={type} value={value} onChange={(event) => onChange(event.target.value)} required /></label>;
}

function EmployeeSelect({ label, value, employees, onChange }: { label: string; value: string; employees: ICompanyEmployee[]; onChange: (value: string) => void }) {
  return <label className="block space-y-1 text-xs text-slate-400"><span>{label}</span><select className={selectClass} value={value} onChange={(event) => onChange(event.target.value)} required><option value="">Select employee</option>{value && !employees.some((employee) => employee.name === value) && <option value={value}>{value}</option>}{employees.map((employee) => <option key={employee._id} value={employee.name}>{employee.name} · {employee.role}</option>)}</select></label>;
}

function LeadEditor({ initial, employees, onSave, onCancel }: { initial: LeadForm; employees: ICompanyEmployee[]; onSave: (data: LeadForm) => Promise<void>; onCancel: () => void }) {
  const [form, setForm] = useState(initial);
  const set = (key: keyof LeadForm, value: string) => setForm((current) => ({ ...current, [key]: value }));
  return <form onSubmit={(event) => { event.preventDefault(); void onSave(form); }} className="grid gap-3 rounded-xl border border-indigo-500/30 bg-slate-900 p-4 md:grid-cols-4">
    <Field label="Lead name" value={form.name} onChange={(value) => set('name', value)} /><Field label="Country" value={form.country} onChange={(value) => set('country', value)} /><Field label="System" value={form.system} onChange={(value) => set('system', value)} /><Field label="Contact" value={form.contactNo} onChange={(value) => set('contactNo', value)} />
    <Field label="Details" value={form.otherDetails} onChange={(value) => set('otherDetails', value)} /><EmployeeSelect label="Connected by" value={form.connectedBy} employees={employees} onChange={(value) => set('connectedBy', value)} />
    <label className="space-y-1 text-xs text-slate-400"><span>Connected</span><select className={selectClass} value={form.connected} onChange={(event) => set('connected', event.target.value)}><option value="yes">Yes</option><option value="no">No</option></select></label>
    <label className="space-y-1 text-xs text-slate-400"><span>Customer type</span><select className={selectClass} value={form.customerType || 'NEW'} onChange={(event) => set('customerType', event.target.value)}><option value="NEW">New</option><option value="EXISTING_CUSTOMER">Existing Customer</option><option value="UPGRADE">Upgrade</option></select></label>
    <label className="space-y-1 text-xs text-slate-400"><span>Converted sale</span><select className={selectClass} value={form.isSale} onChange={(event) => set('isSale', event.target.value)}><option value="yes">Yes</option><option value="no">No</option></select></label>
    <div className="flex gap-2 md:col-span-4"><button className="rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white"><Check className="mr-1 inline h-3.5 w-3.5" />Save</button><button type="button" onClick={onCancel} className="rounded-lg bg-slate-700 px-3 py-2 text-xs text-white">Cancel</button></div>
  </form>;
}

export function LeadsSection({ readOnly = false }: { readOnly?: boolean }) {
  const [leads, setLeads] = useState<ICompanyLead[]>([]);
  const [employees, setEmployees] = useState<ICompanyEmployee[]>([]);
  const [editing, setEditing] = useState<string | null>(null);
  const [adding, setAdding] = useState(false);
  const [employeeFilter, setEmployeeFilter] = useState('');
  const [customerFilter, setCustomerFilter] = useState('');
  const [monthFilter, setMonthFilter] = useState('');
  const [fromDate, setFromDate] = useState('');
  const [toDate, setToDate] = useState('');
  useEffect(() => { companyService.getLeads().then(setLeads).catch(() => toast.error('Unable to load leads')); if (!readOnly) companyService.getEmployees().then(setEmployees).catch(() => toast.error('Unable to load employees')); }, [readOnly]);
  const save = async (data: LeadForm) => { try { const lead = editing ? await companyService.updateLead(editing, data) : await companyService.createLead(data); setLeads((current) => editing ? current.map((item) => item._id === editing ? lead : item) : [lead, ...current]); setEditing(null); setAdding(false); toast.success(editing ? 'Lead updated' : 'Lead created'); } catch (error: any) { toast.error(error.response?.data?.message || 'Unable to save lead'); } };
  const remove = async (id: string) => { if (!window.confirm('Delete this lead?')) return; try { await companyService.deleteLead(id); setLeads((current) => current.filter((lead) => lead._id !== id)); toast.success('Lead deleted'); } catch (error: any) { toast.error(error.response?.data?.message || 'Unable to delete lead'); } };
  const connected = leads.filter((lead) => lead.connected === 'yes').length;
  const converted = leads.filter((lead) => lead.isSale === 'yes').length;
  const filteredLeads = leads.filter((lead) => matchesBusinessDateFilters(lead.createdAt, { employee: employeeFilter, month: monthFilter, from: fromDate, to: toDate, customer: customerFilter }) && (!employeeFilter || lead.connectedBy === employeeFilter) && (!customerFilter || lead.name.toLowerCase().includes(customerFilter.toLowerCase())));
  const exportLeads = () => downloadCsv('leads.csv', [['Lead', 'Country', 'System', 'Contact', 'Connected By', 'Connected', 'Sale'], ...filteredLeads.map((lead) => [lead.name, lead.country, lead.system, lead.contactNo, lead.connectedBy, lead.connected, lead.isSale])]);
  return <div className="min-h-full space-y-6 overflow-y-auto bg-slate-950 p-6 text-slate-100"><header className="flex items-center justify-between border-b border-slate-800 pb-4"><div><h1 className="flex items-center gap-2 text-2xl font-bold text-white"><UserPlus className="h-6 w-6 text-sky-400" /> {readOnly ? 'My Leads' : 'Leads Management'}</h1><p className="text-sm text-slate-400">{readOnly ? 'Leads connected by you.' : 'Create, update, and remove company leads.'}</p></div>{!readOnly && <button onClick={() => { setAdding(true); setEditing(null); }} className="rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white"><Plus className="mr-1 inline h-4 w-4" />Add Lead</button>}</header>
    <div className="flex flex-wrap items-end gap-2 rounded-xl border border-slate-800 bg-slate-900/60 p-3">{!readOnly && <label className="text-xs text-slate-400">Connected by<select value={employeeFilter} onChange={(event) => setEmployeeFilter(event.target.value)} className="ml-2 rounded-lg border border-slate-700 bg-slate-950 px-2 py-1.5 text-white"><option value="">Everyone</option>{employees.map((employee) => <option key={employee._id} value={employee.name}>{employee.name}</option>)}</select></label>}<label className="text-xs text-slate-400">Customer<input type="text" value={customerFilter} onChange={(event) => setCustomerFilter(event.target.value)} placeholder="Search name" className="ml-2 rounded-lg border border-slate-700 bg-slate-950 px-2 py-1.5 text-white" /></label><label className="text-xs text-slate-400">Month & year<input type="month" value={monthFilter} onChange={(event) => { setMonthFilter(event.target.value); setFromDate(''); setToDate(''); }} className="ml-2 rounded-lg border border-slate-700 bg-slate-950 px-2 py-1.5 text-white" /></label><label className="text-xs text-slate-400">Start date<input type="date" value={fromDate} onChange={(event) => { setFromDate(event.target.value); setMonthFilter(''); }} className="ml-2 rounded-lg border border-slate-700 bg-slate-950 px-2 py-1.5 text-white" /></label><label className="text-xs text-slate-400">End date<input type="date" value={toDate} onChange={(event) => { setToDate(event.target.value); setMonthFilter(''); }} className="ml-2 rounded-lg border border-slate-700 bg-slate-950 px-2 py-1.5 text-white" /></label><button type="button" onClick={() => { setEmployeeFilter(''); setCustomerFilter(''); setMonthFilter(''); setFromDate(''); setToDate(''); }} className="rounded-lg bg-slate-700 px-3 py-2 text-xs text-white">Clear</button>{!readOnly && <button onClick={exportLeads} className="ml-auto flex items-center gap-1 rounded-lg bg-emerald-600 px-3 py-2 text-xs font-semibold text-white"><Download className="h-3.5 w-3.5" />Export CSV</button>}</div>
    <div className="grid gap-4 sm:grid-cols-3"><Metric icon={UserPlus} label="Total Leads" value={filteredLeads.length} color="text-sky-400" /><Metric icon={UserCheck} label="Connected" value={`${filteredLeads.filter((lead) => lead.connected === 'yes').length} / ${filteredLeads.length}`} color="text-indigo-400" /><Metric icon={CheckCircle2} label="Converted" value={filteredLeads.filter((lead) => lead.isSale === 'yes').length} color="text-emerald-400" /></div>
    {!readOnly && (adding || editing) && <LeadEditor initial={editing ? (leads.find((lead) => lead._id === editing) as LeadForm) : emptyLead} employees={employees} onSave={save} onCancel={() => { setAdding(false); setEditing(null); }} />}
    <div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full text-left text-xs text-slate-300"><thead className="bg-slate-900 text-slate-400"><tr><th className="p-3">Lead</th><th className="p-3">Country</th><th className="p-3">Contact</th><th className="p-3">Connected</th><th className="p-3">Connected By</th><th className="p-3">Sale</th>{!readOnly && <th className="p-3 text-right">Actions</th>}</tr></thead><tbody className="divide-y divide-slate-800">{leads.map((lead) => <tr key={lead._id}><td className="p-3"><b className="text-white">{lead.name}</b><span className="block text-sky-400">{lead.system}</span></td><td className="p-3"><Globe className="mr-1 inline h-3.5 w-3.5" />{lead.country}</td><td className="p-3"><Phone className="mr-1 inline h-3.5 w-3.5" />{lead.contactNo}</td><td className="p-3">{lead.connected}</td><td className="p-3">{lead.connectedBy}</td><td className="p-3">{lead.isSale}</td>{!readOnly && <td className="p-3 text-right"><button title="Edit lead" onClick={() => { setEditing(lead._id); setAdding(false); }} className="mr-2 text-indigo-400"><Pencil className="inline h-4 w-4" /></button><button title="Delete lead" onClick={() => void remove(lead._id)} className="text-rose-400"><Trash2 className="inline h-4 w-4" /></button></td>}</tr>)}</tbody></table></div>
  </div>;
}

function SaleEditor({ initial, leads, employees, onSave, onCancel }: { initial: SaleForm; leads: ICompanyLead[]; employees: ICompanyEmployee[]; onSave: (data: SaleForm) => Promise<void>; onCancel: () => void }) {
  const [form, setForm] = useState(initial);
  const set = (key: keyof SaleForm, value: string) => setForm((current) => ({ ...current, [key]: key === 'amount' ? Number(value) : value }));
  const selectCustomer = (value: string) => {
    const lead = leads.find((item) => item.name === value);
    setForm((current) => lead ? { ...current, leadId: lead._id, name: lead.name, country: lead.country, system: lead.system, customerEmail: lead.customerEmail || '', customerAddress: lead.customerAddress || '', alternateContactNo: lead.alternateContactNo || '' } : { ...current, name: value });
  };
  return <form onSubmit={(event) => { event.preventDefault(); void onSave(form); }} className="grid gap-3 rounded-xl border border-indigo-500/30 bg-slate-900 p-4 md:grid-cols-4">
    <label className="block space-y-1 text-xs text-slate-400"><span>Customer from Leads</span><input list="company-lead-customers" className={inputClass} placeholder="Search lead customer..." value={form.name} onChange={(event) => selectCustomer(event.target.value)} required /><datalist id="company-lead-customers">{leads.map((lead) => <option key={lead._id} value={lead.name}>{lead.country} - {lead.system}</option>)}</datalist></label>
    <Field label="Customer Email" value={form.customerEmail || ''} onChange={(value) => set('customerEmail', value)} />
    <Field label="Alt Contact No" value={form.alternateContactNo || ''} onChange={(value) => set('alternateContactNo', value)} />
    <Field label="Address" value={form.customerAddress || ''} onChange={(value) => set('customerAddress', value)} />
    <Field label="Country" value={form.country} onChange={(value) => set('country', value)} />
    <Field label="System" value={form.system} onChange={(value) => set('system', value)} />
    <EmployeeSelect label="Closed by" value={form.connectedBy} employees={employees} onChange={(value) => set('connectedBy', value)} />
    <label className="space-y-1 text-xs text-slate-400"><span>Customer type</span><select className={selectClass} value={form.customerType || 'NEW'} onChange={(event) => set('customerType', event.target.value)}><option value="NEW">New</option><option value="EXISTING_CUSTOMER">Existing Customer</option><option value="UPGRADE">Upgrade</option></select></label>
    <Field label="Plan / Package" value={form.plan || ''} onChange={(value) => set('plan', value)} />
    <Field label="Payment Merchant" value={form.paymentMerchant || ''} onChange={(value) => set('paymentMerchant', value)} />
    <Field label="Amount" type="number" value={form.amount} onChange={(value) => set('amount', value)} />
    <Field label="Sale date" type="date" value={form.saleDate} onChange={(value) => set('saleDate', value)} />
    <label className="space-y-1 text-xs text-slate-400"><span>Payment method</span><select className={selectClass} value={form.paymentMethod} onChange={(event) => set('paymentMethod', event.target.value)}>{['Card', 'Check', 'Wire Transfer', 'Cash', 'UPI', 'Bank Transfer', 'Online', 'Other'].map((method) => <option key={method}>{method}</option>)}</select></label>
    <div className="flex items-end gap-2 md:col-span-4"><button className="rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white"><Check className="mr-1 inline h-3.5 w-3.5" />Save</button><button type="button" onClick={onCancel} className="rounded-lg bg-slate-700 px-3 py-2 text-xs text-white">Cancel</button></div>
  </form>;
}

export function SalesSection({ readOnly = false }: { readOnly?: boolean }) {
  const [sales, setSales] = useState<ICompanySale[]>([]);
  const [leads, setLeads] = useState<ICompanyLead[]>([]);
  const [employees, setEmployees] = useState<ICompanyEmployee[]>([]);
  const [editing, setEditing] = useState<string | null>(null);
  const [adding, setAdding] = useState(false);
  const [employeeFilter, setEmployeeFilter] = useState('');
  const [customerFilter, setCustomerFilter] = useState('');
  const [monthFilter, setMonthFilter] = useState('');
  const [fromDate, setFromDate] = useState('');
  const [toDate, setToDate] = useState('');
  useEffect(() => {
    companyService.getSales().then(setSales).catch(() => toast.error('Unable to load sales'));
    companyService.getLeads().then(setLeads).catch(() => toast.error('Unable to load lead customers'));
    if (!readOnly) companyService.getEmployees().then(setEmployees).catch(() => toast.error('Unable to load employees'));
  }, [readOnly]);
  const save = async (data: SaleForm) => { try { const sale = editing ? await companyService.updateSale(editing, data) : await companyService.createSale(data); setSales((current) => editing ? current.map((item) => item._id === editing ? sale : item) : [sale, ...current]); setEditing(null); setAdding(false); toast.success(editing ? 'Sale updated' : 'Sale created'); } catch (error: any) { toast.error(error.response?.data?.message || 'Unable to save sale'); } };
  const remove = async (id: string) => { if (!window.confirm('Delete this sale?')) return; try { await companyService.deleteSale(id); setSales((current) => current.filter((sale) => sale._id !== id)); toast.success('Sale deleted'); } catch (error: any) { toast.error(error.response?.data?.message || 'Unable to delete sale'); } };
  const normalSales = sales.filter((sale) => !sale.failed);
  const revenue = normalSales.reduce((sum, sale) => sum + sale.amount, 0);
  const filteredSales = normalSales.filter((sale) => (!employeeFilter || sale.connectedBy === employeeFilter) && (!customerFilter || sale.name.toLowerCase().includes(customerFilter.toLowerCase()) || (sale.customerId && sale.customerId.includes(customerFilter))) && matchesBusinessDateFilters(sale.saleDate, { employee: employeeFilter, month: monthFilter, from: fromDate, to: toDate, customer: customerFilter }));
  const exportSales = () => downloadCsv('sales.csv', [['Customer ID', 'Customer', 'Email', 'Alt Phone', 'Address', 'Country', 'System', 'Plan', 'Merchant', 'Closed By', 'Payment', 'Date', 'Amount'], ...filteredSales.map((sale) => [sale.customerId || 'N/A', sale.name, sale.customerEmail || '', sale.alternateContactNo || '', sale.customerAddress || '', sale.country, sale.system, sale.plan || '', sale.paymentMerchant || '', sale.connectedBy, sale.paymentMethod, sale.saleDate, String(sale.amount)])]);
  const salesFilterBar = <DateFilterBar filters={{ employee: employeeFilter, month: monthFilter, from: fromDate, to: toDate, customer: customerFilter }} setFilters={(next) => { setEmployeeFilter(next.employee); setCustomerFilter(next.customer); setMonthFilter(next.month); setFromDate(next.from); setToDate(next.to); }} employees={employees} employeeLabel="Closed by" readOnly={readOnly} />;
  return <div className="min-h-full space-y-6 overflow-y-auto bg-slate-950 p-6 text-slate-100"><header className="flex items-center justify-between border-b border-slate-800 pb-4"><div><h1 className="flex items-center gap-2 text-2xl font-bold text-white"><TrendingUp className="h-6 w-6 text-emerald-400" /> {readOnly ? 'My Sales' : 'Sales Revenue'}</h1><p className="text-sm text-slate-400">{readOnly ? 'Sales completed by you with full customer details.' : 'Manage company sales transactions and customer details.'}</p></div>{!readOnly && <button onClick={() => { setAdding(true); setEditing(null); }} className="rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white"><Plus className="mr-1 inline h-4 w-4" />Add Sale</button>}</header><div className="grid gap-4 sm:grid-cols-3"><Metric icon={TrendingUp} label="Revenue" value={`$${revenue.toLocaleString()}`} color="text-emerald-400" /><Metric icon={CreditCard} label="Closed Deals" value={sales.length} color="text-indigo-400" /><Metric icon={Target} label="Average Deal" value={`$${sales.length ? Math.round(revenue / sales.length).toLocaleString() : 0}`} color="text-amber-400" /></div>{salesFilterBar}{(adding || editing) && <SaleEditor initial={editing ? (sales.find((sale) => sale._id === editing) as SaleForm) : emptySale} leads={leads} employees={employees} onSave={save} onCancel={() => { setAdding(false); setEditing(null); }} />}<div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full text-left text-xs text-slate-300"><thead className="bg-slate-900 text-slate-400"><tr><th className="p-3">Customer & ID</th><th className="p-3">Contact & Address</th><th className="p-3">System & Plan</th><th className="p-3">Closed by</th><th className="p-3">Merchant & Mode</th><th className="p-3">Date</th><th className="p-3 text-right">Amount / Actions</th></tr></thead><tbody className="divide-y divide-slate-800">{filteredSales.map((sale) => <tr key={sale._id} className="hover:bg-slate-900/40 transition-colors"><td className="p-3"><div className="flex items-center gap-1.5"><b className="text-white text-sm">{sale.name}</b>{sale.customerId && <span className="rounded bg-indigo-500/20 px-1.5 py-0.5 text-[10px] font-mono font-bold text-indigo-300 border border-indigo-500/30">#{sale.customerId}</span>}</div><span className="block text-slate-400 text-[11px]">{sale.country}</span></td><td className="p-3 text-[11px]"><div className="text-slate-300">{sale.customerEmail || '—'}</div>{sale.alternateContactNo && <div className="text-emerald-400">Alt: {sale.alternateContactNo}</div>}{sale.customerAddress && <div className="text-slate-500 truncate max-w-xs">{sale.customerAddress}</div>}</td><td className="p-3"><div className="font-semibold text-slate-200"><Building2 className="mr-1 inline h-3.5 w-3.5 text-slate-400" />{sale.system}</div>{sale.plan && <div className="text-[11px] text-cyan-400">{sale.plan}</div>}{sale.issues && <div className="text-[10px] text-slate-500 truncate max-w-[180px]" title={sale.issues}>Issue: {sale.issues}</div>}</td><td className="p-3">{sale.connectedBy}</td><td className="p-3"><div><span className="font-medium text-slate-200">{sale.paymentMethod}</span></div>{sale.paymentMerchant && <span className="text-[10px] text-slate-400 block font-mono">Via: {sale.paymentMerchant}</span>}</td><td className="p-3 text-slate-400">{sale.saleDate}</td><td className="p-3 text-right"><span className="mr-3 font-mono font-bold text-emerald-400 text-sm">${sale.amount.toLocaleString()}</span>{!readOnly && <><button title="Edit sale" onClick={() => { setEditing(sale._id); setAdding(false); }} className="mr-2 text-indigo-400"><Pencil className="inline h-4 w-4" /></button><button title="Delete sale" onClick={() => void remove(sale._id)} className="text-rose-400"><Trash2 className="inline h-4 w-4" /></button></>}</td></tr>)}</tbody></table></div></div>;
}


function Metric({ icon: Icon, label, value, color }: { icon: typeof TrendingUp; label: string; value: string | number; color: string }) { return <div className="rounded-xl border border-slate-800 bg-slate-900/60 p-5"><Icon className={`h-5 w-5 ${color}`} /><p className="mt-3 text-xs uppercase text-slate-500">{label}</p><p className="mt-1 text-2xl font-bold text-white">{value}</p></div>; }
